# Issue implementation matrix

“Gate” means implementation and a reproducible job exist, but closure needs CI
history, specific hardware, a release tag, or an external service.

| Issues | Implementation / evidence |
|---|---|
| #8, #15, #16, #19, #63 | mmap/versioned postcard graph store, parallel stat/hash cache, append-only journal, torn-tail and kill tests |
| #9, #10, #12, #13 | captured executor, env whitelist, critical-path/fifo scheduler, progress, keep-going, failure summary |
| #18 | structured progress events plus a TTY-only live dashboard with job slots, cache/timing state, critical path, scrollable logs, plain pipe/CI fallback and `--no-tui`; PTY/plain E2E and renderer overhead gate |
| #14, #21, #25, #44 | benchmark runner and PR/nightly artifacts exist. #25's equivalent 10k linear-graph median-of-31 measured 2.271 ms end-to-end daemon CLI and 0.203 ms direct socket versus Ninja 58.556 ms (25.78x), with raw samples and reproduction command checked in. Watcher proofs require one full certificate validation plus normal in-workspace evidence; event barriers invalidate on source/manifest/`.frost` output changes, while external/symlink/missing evidence, arbitrary dynamic env, watcher errors or timeouts stay on the complete check/fallback path. The same report records the losing leaf-change result (411.679 ms versus Ninja 63.338 ms) rather than extending the no-op claim |
| #17, #20, #50, #56 | escaped depfiles, dynamic headers, early cutoff, path policy/E2E, generated order-only edges |
| #22, #23, #24 | framed/versioned socket, recursive watcher, lifecycle commands, serialized builds, correctness-preserving engine validation/fallback |
| #26, #28, #29 | immutable BLAKE3 CAS with GC, cached compiler closure, bubblewrap sandbox and undeclared-header E2E |
| #31, #32, #36, #37 | action pruning stats, affected tests, safe predictive flag, scheduler/estimator flags; ML reduction gated on replay miss rate |
| #34, #35 | REAPI cache decision and Rust Ninja subset importer |
| #39–#43 | DESIGN/manifest/README/contribution docs, six crates, toolchain pin, forms and milestone assignment |
| #45, #46 | four fuzz targets, proptest, nightly fuzz, cargo-deny, Dependabot and SHA-pinned actions |
| #47 | Completed by v0.1.0: SemVer/CHANGELOG/install docs and tag-triggered musl binaries/checksums. v0.3.0 extends release archives to macOS and Windows |
| #48, #49, #51 | process-group cancellation, partial-output cleanup, explain/trace and compilation database |
| #52–#54 | platform/language decisions and authoritative Rust/reference Python/historical Zig roles |
| #57–#61 | profiles, C++, hermetic globs, multi-package labels and cached test runner |
| #62 | deterministic double-run mode and macro/output diagnosis E2E |
| #64 | Completed by the 2026-07-28 external certificate. A BuildGrid 0.8.4 / BuildBox action proved deterministic SHA-256 Merkle inputs, `Command`/`Action`, platform properties, missing-blob identification and recovery, nested output `Tree` verification and an Action Cache hit. The checked client, fixture, environment and raw digests are in `scripts/reapi_poc.py`, `tests/fixtures/reapi` and `bench/baselines/2026-07-28-buildgrid-reapi-poc.json`; the remaining work is an isolated v2 adapter, not a v1 compatibility correction |
| #81 | Completed in v0.3.0. The mutex-contention hypothesis was falsified by counters; the real linear-chain costs were thundering-herd wakeups and per-action checking. Targeted wakeups plus a checksummed whole-closure no-op certificate avoid that work, with corruption/change fallback tests and a published 10k standalone median 15.620 ms versus Ninja 42.419 ms |
| #82 | Completed as a measured defer decision. The local verified core remains shipped. Fresh Lua and ripgrep v2 corpora have zero verification failures and break even at 23.787799 and 4.668801 Mbit/s respectively. A tested calibration combines exact bytes/CPU with observed BuildGrid RPC/REAPI 2.2 behavior at 1/10/100/1000 Mbit/s. Remote DeltaCDC stays off by default until CPU falls, a deployed protocol negotiates chunk deltas and production traces are below the relevant break-even |
| #83 | Completed in v0.3.0. Bazel-bit-compatible FastCDC 2020 boundaries/defaults, >2 MiB threshold, SHA-256 chunk store, versioned blob manifests, verified chunk splice fallback and persistent `cache stats` reuse ratios. E2E restores without the whole blob, retains >75% of chunks after one byte changes, and bounded parallel chunk work measured 1.41x faster cold publication / 1.89x faster restore in the checked 64 MiB A/B |
| #84 | Completed in v0.3.0. The `UnverifiedBytes`/`VerifiedBlob` publication boundary plus a required published CI job inject bit flips, missing/wrong/truncated/single chunks, ordering changes, parameter mismatch, delta failures and the Bazel #29544 final-path scenario |
| #87 | Completed by bounded production adoption. `import-npm --vite-builds` conservatively emits profile-specific `output_dirs` only for recognized non-watch Vite builds, while build cannot be mislabeled as a test. On read-only `iroha-pdf@10bf0ac`, seven validation/typecheck targets passed, the 6.26 MB production tree built, a no-op took 0 ms, and complete-tree removal restored seven byte-identical files from CAS in 10 ms. Persistent HMR stays framework-owned, `node_modules` stays explicitly npm-owned/non-hermetic, and source maps remain producer-declared with truthful Node IDE handoff |
| #105 | Daemon builds apply the invoking client's environment verbatim; a resident daemon honours shutdown at any protocol version and a version mismatch falls back to an in-process build. The E2E regression fails without the fix (43 versus 141) |
| #107 | `output_dirs` lets a command target own directories: post-execution deterministic scan, journalled per-file digests, CAS publication, exact tree restoration, a content stamp as the graph node for edges and early cutoff, and action-key schema v4. Symlinked and overlapping ownership are configuration errors |
| #109 | `depfile_format` dispatches `make` / `lines` / `showincludes`; MSVC notes are read from captured output, stripped from the log and parsed after the last `: ` so a localized toolchain works. The `lines` path has an E2E; `showincludes` has parser unit tests until a Windows MSVC job exists |
| #110 | macOS runs the whole workspace suite, and Windows runs all 42 host-reachable E2Es serially with MinGW. Exclusions are declared in the tests (`cfg(unix)`, `cfg(target_os = "linux")`, bubblewrap early return, JDK/Rust toolchain consistency) and tabulated in docs/09. The Windows run covers host executable suffixes, a `${pathsep}`-portable paired sample launcher, workspace-relative `.exe` spawning, non-inheriting daemon startup and the action environment required by Go |
| #111 | Property gates gate arbitrarily corrupted CAS chunk manifests and no-op certificates on every pull request. Libfuzzer targets for both remain open because neither decoder is reachable from a byte slice without widening the public API |
| #112 | docs/README.md indexes every document by purpose and records why the duplicated numeric prefixes stay |
| #108 | `--remote-cache` / `--remote-upload` over a shared directory or plain HTTP. Declared-input trace key with the producing run's discovered inputs verified on the consumer, digest-verified responses with the mode recovered from the digest, publication through the existing `LocalCas` boundary, and fallback to local execution on every failure. Measured: a cold workspace built from the shared cache in 2 ms with nothing executed. REAPI protobuf/gRPC, HTTPS, `FindMissingBlobs` batching and chunk-level transfer are open |
| #130 | User and workspace `.frostrc` files, ordered `[common]` / subcommand / repeatable `[config.NAME]` layers, `--no-frostrc`, strict diagnostics, `doctor` provenance, and action-key-equivalent CLI expansion. No `FROST_*` option exists to populate the reserved environment layer; adding such a configuration surface remains a separate compatibility decision |
| #141 | Root-level pinned HTTP(S) tar.gz/ZIP fetch declarations, SHA-256-before-CAS verification, link/special/path-traversal-safe extraction, same-filesystem staged publication and ownership state. `frost fetch` is no-op by default, supports explicit `--force` and network-free `--offline`; builds never fetch and diagnose missing state. Materialized tree files feed dependent action keys; E2Es cover mismatch atomicity, no-op/force request counts, offline/missing diagnostics and rebuild correctness |
| #143 | `frost test --coverage` builds an isolated `<profile>+coverage` graph, objects, journal and raw-counter tree; fingerprints `gcov`; merges one deterministic lcov tracefile per `cc_test`; and keys the merge from counter contents plus `.gcno` notes. Unit tests pin graph/key/store separation, while gcc/gcov E2Es pin ordinary-cache isolation, byte-identical reruns and affected-test-only remerging |
| #158 | `${dep:LABEL}` / `${deps:LABEL}` resolve declared dependency outputs in direct argv, test steps, environment values and genrule command strings. Ambiguous multi-output and directory-only targets fail at load time; moved outputs change action-key material; unit and E2E tests cover the complete surface. A general path-list join expression remains deliberately out of scope |
| #155 | Target `resources = { cpu, ram_mb, exclusive }` declarations feed one shared deterministic admission model in the real executor and `simulate`; host-derived CPU/RAM defaults, explicit CLI budgets, oversized-action progress, test-only concurrency, resource peaks in `--stats`, and key exclusion are covered by manifest/graph/simulation/key tests plus a real two-test concurrency E2E |
| #180 | The in-repository VS Code extension keeps daemon health and the last build result as independent status-bar state. It polls the versioned `frost daemon status --json` contract, distinguishes stopped/running/protocol mismatch, supports manual refresh, and stays quiet for missing/older binaries; Rust E2E and TypeScript parser/editor tests cover both sides |

The v0.3.0 release and its Linux/macOS/Windows archives provide the publication
gate for #81, #83 and #84. The 28 July 2026 external/repository certificates
close the evidence gates for #64, #82 and #87 without expanding their claims:
one external REAPI implementation, two artifact corpora, one production npm
monorepo and one recorded host. The documents preserve the losing bandwidth
cases and all deliberate non-hermetic/framework-owned boundaries.
