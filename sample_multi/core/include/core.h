#ifndef FROST_SAMPLE_MULTI_CORE_H
#define FROST_SAMPLE_MULTI_CORE_H

/* The generated version string, as the rest of the workspace sees it. */
const char *frost_core_version(void);

int frost_core_add(int a, int b);

#endif
