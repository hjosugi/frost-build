# One of the two middles of the diamond. Depends on //core:core only.

[target.text]
kind = "cc_library"
srcs = ["src/text.c"]
deps = ["//core:core"]
# One of the two routes from //apps/cli to //core.
visibility = ["//apps/..."]
includes = ["include"]
