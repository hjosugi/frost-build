#ifndef FROST_SAMPLE_MULTI_TEXT_H
#define FROST_SAMPLE_MULTI_TEXT_H

/* Writes "frost <version>" into `out`, which must hold `cap` bytes. */
void frost_text_banner(char *out, unsigned long cap);

#endif
